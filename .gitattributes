# Normalize line endings to LF in the repo for all text files.
# Keeps diffs clean across Windows/macOS/Linux and on GitHub.
* text=auto eol=lf

# Treat these as binary (never normalize) if added later.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
